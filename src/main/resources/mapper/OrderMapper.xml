<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ecommerce.mapper.OrderMapper">
    <insert id="insertOrder" parameterType="com.example.ecommerce.model.Order">
        insert into `order`(customer_name,customer_email,total_price,created_at)
        values(#{customerName},#{customerEmail},#{totalPrice},#{createdAt})
    </insert>
</mapper>